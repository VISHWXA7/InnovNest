const authConfig = {
    providers: [
        {
            domain: "https://allowing-boxer-69.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
    
};

export default authConfig;